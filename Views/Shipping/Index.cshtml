@model IEnumerable<ShippingModels>

<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-5">
        <h1>Transportadora</h1>
        <a class="btn btn-success mx-2" asp-action="Create"><i class="bi bi-file-earmark-plus mx-2"></i>Cadastrar nova Transportadora</a>
    </div>

    @if (TempData["MensagemSucesso"] != null)
    {
        <div class="alert alert-success text-light" role="alert">
            @TempData["MensagemSucesso"]
        </div>
    }

    <table id="Shipping" class="table table-light table-hover mt-5">
        <thead>
            <tr>
                <th scope="col">Transportadora</th>
                <th scope="col">Cnpj</th>
                <th scope="col">Nome</th>
                <th scope="col">Cpf</th>
                <th scope="col">Telefone</th>
                <th></th>
            </tr>
        </thead>
        <tbody>

            @if (Model != null)
            {
                @foreach (var shipping in Model)
                {
                    <tr>
                        <td>@shipping.sp_corporate_name</td>
                        <td>@shipping.sp_cnpj</td>
                        <td>@shipping.sp_responsible_name</td>
                        <td>@shipping.sp_responsible_cpf</td>
                        <td>@shipping.sp_responsible_phone</td>
                        <td>
                            <a class="btn btn-warning mx-2" asp-controller="Shipping" asp-action="Edit" asp-route-id="@shipping.sp_id"><i class="bi bi-pencil-square mx-2"></i>Editar</a>
                            <a class="btn btn-danger mx-2" asp-controller="Shipping" asp-action="Delete" asp-route-id="@shipping.sp_id"><i class="bi bi-trash mx-2"></i>Excluir</a>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>

</div>